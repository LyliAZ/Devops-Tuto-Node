name: 'npm'
description: 'An action to create an npm package and push it to artifactory'

inputs:
  npm_repo:
    required: true
  secret:
    description: 'artifatory npm secret'
    required: true
runs:
  using: "composite"
  steps:
    - name: Log in to the npm registry
      uses: actions/setup-node@v4
      with:
        node-version: '16.x'
    - run: 
       |
        echo "email = lylia.ait.zerrouk@gmail.com
              always-auth = true
              registry=https://trialk033s3.jfrog.io/artifactory/api/npm/paris-npm/
              //trialk033s3.jfrog.io/artifactory/api/npm/paris-npm/:_authToken=${{ inputs.secret }}" > ~/.npmrc |
    - run: npm publish --registry $ARTIFACTORY_URL/artifactory/api/npm/paris-npm/
